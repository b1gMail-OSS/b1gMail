# SabreDAV for b1gMail

Composer-managed [sabre/dav](https://github.com/sabre-io/dav) 4.7 for CalDAV, CardDAV and WebDAV.

```
composer update --no-dev
```

Application backends live in `interface/caldav.php`, `interface/carddav.php`, `interface/webdav.php` and `serverlib/dav-real.inc.php`.

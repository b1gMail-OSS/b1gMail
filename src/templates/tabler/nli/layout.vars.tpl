{* Smarty 5: assigns in includes are local unless scope=parent on the {include} tag *}
{assign var="nliStyle" value=$templatePrefs.loginStyle|default:'cover'}
{assign var="nliCompactLayout" value=true}
